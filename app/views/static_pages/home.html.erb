<% if user_signed_in? %>
  <% redirect_to crops_path %>
<% else %>
<div class="jumbotron">
  <div class="container">

    <h1 class="center">Welcome!</h1>
    <p class="center">This is a farm planning web application designed to help small farmers and gardeners generate a planting schedule, including a seperate greenhouse schedule, to manage the planting season.<br>
       <%= link_to "Sign In", new_user_session_path, :class => "btn btn-large btn-primary" %>
       <%= link_to "Sign Up", new_user_registration_path, :class => "btn btn-large btn-primary" %>
  </div>
</div>
<% end %>

